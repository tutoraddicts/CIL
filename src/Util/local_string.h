#pragma once

#ifndef LOCAL_STRING_H
#define LOCAL_STRING_H

#include <string.h>
/*
 * Local String Functions to handle string because it may be going too far who knows
 */
    // typedef char* string;
    
    int StringLen(char[]); // function to get string lenth
#endif